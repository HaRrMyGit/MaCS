===Утв:=== $\psi: G \to G'$ - гомоморфизм => $Ker\psi$ - подгруппа $G$
Док-во:
	1. $z, \tilde z \in Ker\psi$    $\psi (z, \tilde z) = \psi(z) \cdot \psi(\tilde z) = e => z\tilde z \in Ker\psi$
	2. Пусть $z \in Ker\psi$  $\psi(z^{-1}) = (\psi(z))^{-1} = e^{-1} = e$ =>
	   $z^{-1} \in Ker\psi$

===Утв:=== $G\curvearrowright X$ Тогда $\forall x \in X$. $Stabx$ - подгр $G$
Док-во:
	1. $g, \tilde g \in Stabx$ Почему $g \cdot \tilde g \in Stabx$?
	   $\varphi g \tilde g(x) = (\varphi g\circ \varphi \tilde g)(x) = x$
	2. $g \in Stabx$ Почему $g^{-1} \in Stabx$
	   $\varphi g^{-1}(x) = (\varphi g)^{-1}(x) = x$ 
===Утв:=== $G \curvearrowright X$ Тогда орбиты либо не пересекаются, либо совпадают
Док-во:
	Пусть $y \in Orbx \cap Orb\tilde x$
	$y = \varphi gx = \varphi \tilde g \tilde x$ Покажем, что $\forall t \in OrbX$ лежит в $Orb \tilde x$
	Хочу $t = \varphi g'\tilde x$ А знаю $t \in Orbx, t = \varphi g^\bigtriangleup x$
	$t = \varphi g^\bigtriangleup x = \varphi g^\bigtriangleup((\varphi g)^{-1} \varphi \tilde g \tilde x) = \varphi g^\bigtriangleup \varphi g^{-1} \varphi \tilde g \tilde x = \varphi g^\bigtriangleup g^{-1} \tilde g \tilde x$

===Утв:=== $|Stabx|\cdot|Orbx| = |G|$
Док-во:
	$\sum_{y\in Orbx}{|\{g \in G|\varphi g (x) = y\}|} = |G|$
	$\{g \in G|\varphi g (x) = y\} \gets^f Stabx$
	Зафиксируем $\tilde g \in \{g \in G |\varphi g (x) = y\}$
	$f(z) = \tilde gz$    $\varphi \tilde g z = \varphi \tilde g \cdot \varphi z (x) = \varphi \tilde g(x) = y$
	$f$ - инъекция. Пусть $z, \tilde z \in Stabx$
	$\tilde gz = f(z) = f(\tilde z) =\tilde g \tilde z => z = \tilde z$
	$f$ - сюръекция. Пусть $g' \in \{g \in G|\varphi g(x) = y\}$
	Хочу $g' = \tilde g z$
	$z = \tilde g^{-1} g$'  $\varphi z(x) = \varphi \tilde g^{-1} g'(x) = (\varphi \tilde g)^{-1} \cdot \varphi g'(x) = x$ =
	= $\sum_{y \in Orbx}{|Stabx|} = |Stabx|\cdot|Orbx|$


P.s. $g^\bigtriangleup$ это g с домиком(тут нельзя ^^ делать(()
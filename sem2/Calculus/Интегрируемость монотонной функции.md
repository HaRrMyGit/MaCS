#Глава_4 
#Параграф_2 
#Теорема 
# Формулировка
Монотонная на отрезке функция [[Интегрируемая функция по Риману|интегрируема]] на нём
# Доказательство
$\sqsupset f$ возрастает на $[a,b]$, если $f(a) = f(b)$, то $f = const \Rightarrow f \in R[a,b]$ по [[Интегрируемость непрерывной функции|этой теореме]]
$\sqsupset f(a) < f(b)$, возьмём $\varepsilon > 0$ и подберем $\delta = \frac{\varepsilon}{f(b)-f(a)} > 0$, $\forall T: \lambda_T < \delta$ в силу возрастания $f$:
$$
w_k(f) = f(x_{k+1}) - f(x_k) \Rightarrow \sum^{n-1}_{k=0}w_k(f)\Delta x_k = \sum^{n-1}_{k=0}(f(x_{k+1})-f(x_k))\Delta x_k < 
$$
$$
< \frac{\varepsilon}{f(b)-f(a)}\sum^{n-1}_{k=0}(f(x_{k+1})-f(x_k)) = \frac{\varepsilon}{f(b)-f(a)} \cdot(f(b)-f(a)) = \varepsilon
$$
$$
\square
$$
$R[a,b]$ - [[Класс интегрируемых на отрезке функций]]
$T$ - [[Дробление]]
$\lambda_T$ - [[Ранг дробления]]